<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <div class="container">
    <ul class="navbar-nav">
      <li class="nav-item">
        <% if controller_name == 'users' %>
          <a class="nav-link active">用户</a>
        <% else %>
          <a class="nav-link" href="<%= users_path %>">用户</a>
        <% end %>
      </li>
      <li class="nav-item">
        <% if controller_name == 'messages' %>
          <a class="nav-link active">留言</a>
        <% else %>
          <a class="nav-link" href="<%= messages_path %>">留言</a>
        <% end %>
      </li>
      <li class="nav-item">
        <% if controller_name == 'comments' %>
          <a class="nav-link active">评论</a>
        <% else %>
          <a class="nav-link" href="<%= comments_path %>">评论</a>
        <% end %>
      </li>
      <li class="nav-item dropdown">
        <% if controller_name == 'notifications' %>
          <a class="nav-link dropdown-toggle active" href="#" id="navbardrop" data-toggle="dropdown">
            公告
          </a>
        <% else %>
          <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
            公告
          </a>
        <% end %>
        <div class="dropdown-menu">
          <a class="dropdown-item" href="<%= new_notification_path %>">发布公告</a>
          <a class="dropdown-item" href="<%= notifications_path %>">所有公告</a>
        </div>
      </li>
      <li class="nav-item">
        <%= link_to "退出", logout_path, class:'nav-link', data: {confirm: "确定退出吗？"}%>
      </li>
    </ul>
  </div>
</nav>
